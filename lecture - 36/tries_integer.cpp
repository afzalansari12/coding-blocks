#include<iostream>
using namespace std;
void insert(int n){
    node* cur = root;
    for(int i = 31; i >= 0; i--){
        int ithbit == (n>>i)&1
    }
}
bool search(int n){
    node* cur = root;
    for(int i = 31; i>= 0; i--){
        int ithbit = (n >> i)&1;
        if(ithbit == 0){
            if(cur ->)
        }
    }
}
int main(){
    int arr[] ={2, 4, 8, 5,10};
    int n = sizeof(arr)/sizeof(int);

    trie t;
    for(int i = 0; i < n; i++){
        t.insert(arr[i]);

    }
    int brr[] ={2, 4, 8, 5,10};
    int n = sizeof(brr)/sizeof(int);

for()

    return 0;
}
