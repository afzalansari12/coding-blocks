#include<iostream>
using namespace std;

int* resize(int* A, int C){

    cout << " resizing from"
         << c*sizeof(int) << " B to"
         <<  2*Csizeof(int) << "B" << endl;

         int* B = new int[2*c];
         for (int i = 0; i <c; i++){
            B[i] = A[i];
         }
}

int main(){
  int* A = new int( 1);
  int c = 11;
  int i = 0;
}