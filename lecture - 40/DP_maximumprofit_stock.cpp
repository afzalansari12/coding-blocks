#include<iostream>
#include<vector>

using namespace std;
f(const vector<int>& p ,int n,int i , int j){

    if(i = n|| k == 0){
        return 0;
    }
    for(int i = 0; j <= n; i++){
          
    }
  
}
int bottomup(const vector<int> & p, int n, int k){
    vector<vector<int>> dp( n + 1,vector<int>(k + 1, 0));
    for(int i = n ; i > = 0; i--){
        for(int k = 1; k <= k; k++){
            int dontbuyoption = dp[i + 1][k];
            int

        }
    }
    return dp[0][k];
}
int main(){
   vector<int> p = {3,2,6,5,0,3};
   int n = p.size();
   int K = 2;
   int k;

 cout << f(n,p) << endl;
return 0;
}